module Top
    submodule w : do_while_hello
end module

module do_while_hello
    
    behavior
    do 
        $cout << "Hello world" << endl;$;
        wait(1, 0);
    while (this_cycle != 10) end do;
    end behavior

end module

